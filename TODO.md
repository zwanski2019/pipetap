# pipetap TODO

Apart from general bug fixes and maintenance, the following is a list of things I think pipetap needs:

- The control channel client needs a bus tied to a PID so that the proxy and replay features can function at the same time using the same control channel.
- The GUI needs a settings pane to configure font, theme, etc.
- The support DLL needs a management feature to enable things like the TCP -> pipe proxy.
- Explore a minifilter approach to the same implementation as the support DLL.
